import Vue from 'vue';

const state = {
    clientHeight: 100,
};

const getters = {
    clientHeight: state => state.clientHeight,
};

const actions = {
    
};

const mutations = {

};

export default {
    state,
    getters,
    actions,
    mutations
}